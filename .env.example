# Example environment variables for wiki service
# Copy to .env and adjust values

# PostgreSQL connection string
DATABASE_URL=postgres://user:password@localhost:5432/wiki?sslmode=disable

# HTTP listen address
HTTP_ADDR=:8080
